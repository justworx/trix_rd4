{
	
	#  --- Console ---
	
	# TITLE
	"title" : "Console - The basic application console",
	
	# ABOUT
	"about" : """
		Welcome to the interactive Console. Enter "help" to view a list
		of commands/options.
		""",
	
	# CLOSING
	"closing" : "Console Exit. Returning to previous prompt.",
	
	# HELP
	"help" : {
		"exit" : "Close this console and return to the previous prompt.",
		"help" : "Display help messages.",
		"plugins" : "List active plugins.",
		"wrappers" : "List available wrappers.",
		"wrapper" : "Use a wrapper console; Eg, `wrapper jconfig x.json`"
	},
	
	# MESSAGES
	"messages" : {
		"unknown-command" : "Unknown Command - Not Handled",
		"unknown-wrapper" : "No such wrapper"
	},
	
	
	#
	# PLUGINS
	#  - Plugin keys may be replaced with suitable non-english words.
	#
	"plugins" : {
		"calc": {
			"plugin": "trix.app.plugin.calc.Calc"
		},
		"dir" : {
			"plugin": "trix.app.plugin.dir.Dir"
		}
	},
	
	#
	# WRAPPERS
	#  - Dict where key strings will act as the command to open a
	#    Wrapper console (with it's own "sub-prompt"). 
	#
	"wrappers" : {
		"jconfig" : {
			# to be sent as arg1 from Console to Wrapper
			"nconfig" : "app/config/console/en.jconfig.conf",
			
			# for 'help' in Console
			"help" : "Enter 'jconfig <PATH>' to view/edit a config file."
		}
	},
	
	#
	# TECHNICAL DEFAULTS
	#
	
	# encoding values
	'encoding' : 'utf_8',
	'errors' : 'replace'
}
